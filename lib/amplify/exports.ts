// Placeholder. Replace with your Amplify generated configuration.
// If you run `amplify init` and `amplify add hosting`, Amplify will create a `aws-exports.js`.
// We use a TS wrapper here so imports are typed.

const awsExports = {
  aws_project_region: process.env.NEXT_PUBLIC_AWS_REGION || '',
};

export default awsExports;
